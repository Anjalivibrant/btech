#include "Header.h"//user-defined header file we use ""
using namespace std;//even namespace std is not required here cause our header file already has that
int main()
{
  cout<< add(4,7);
  Class_A obj1;//we created a class in the header file
}